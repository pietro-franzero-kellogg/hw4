<h1>Places</h1>

<div class="py-3">
  <% if @places %>
    <ul>
      <% for place in @places %>
        <li><a href="/places/<%= place["id"] %>"><%= place["name"] %></a></li>
      <% end %>
    </ul>
  <% end %>
</div>

<p>
  <a class="btn btn-primary" href="/places/new">New place</a>
</p>